# Copy to `.env` at this repository root (same folder as this file).
# The app loads: 1) `<repo>/.env`  2) `HW2/.env` (optional override).

OPENAI_API_KEY=sk-your-key-here

# Optional
# OPENAI_MODEL=gpt-4o-mini
# PATIENTS_DB=/absolute/path/to/patients.db
# HW2_QC_TRIALS=5
