# HW2/out/ holds qc_results.csv, qc_summary.md, reports, immutable qc_batches,
# and ai_qualitative_review_* outputs — intentionally tracked for assignments;
# do not add a blanket `out/` or `**/out/` rule without negations.

.DS_Store
# Leftover unzip / scratch dirs
HW2/tmp_app_extract/

# Posit rsconnect-python local deployment metadata (machine-specific)
HW2/rsconnect-python/

**/.venv/
**/__pycache__/
.env
**/.env
!.env.example
HW2.zip
